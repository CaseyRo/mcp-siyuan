__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.venv/
.env
# *.lock intentionally NOT excluded — uv.lock is committed for reproducible builds
# Only exclude lock files from old toolchains if needed:
package-lock.json
yarn.lock
.pytest_cache/
.ruff_cache/
# Internal agent memory & worktrees (infra notes, not for distribution)
.claude/agent-memory/
.claude/worktrees/
